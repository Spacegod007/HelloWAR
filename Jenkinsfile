pipeline {
    agent { dockerfile true }
    stages {
        stage('Build') {
            steps {
                echo 'building'
            }
        }
        stage('Deploy') {
             steps {
                 echo 'Deploying'
             }
        }
    }
}

